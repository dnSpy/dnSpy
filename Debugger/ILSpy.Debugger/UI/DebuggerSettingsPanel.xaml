<UserControl x:Class="ICSharpCode.ILSpy.Options.DebuggerSettingsPanel"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<Grid>
		<StackPanel Margin="10">
			<CheckBox IsChecked="{Binding AskForArguments}">Ask for arguments and working directory before executing a process</CheckBox>
			<CheckBox IsChecked="{Binding BreakAtBeginning}">Break debugged process at start or attach</CheckBox>
			<CheckBox IsChecked="{Binding EnableJustMyCode}">Enable just my code</CheckBox>
			<CheckBox IsChecked="{Binding StepOverDebuggerAttributes}">Step over debugger attributes</CheckBox>
			<CheckBox IsChecked="{Binding StepOverAllProperties}">Step over all properties</CheckBox>
			<CheckBox IsChecked="{Binding StepOverSingleLineProperties}">Step over single line properties</CheckBox>
			<CheckBox IsChecked="{Binding StepOverFieldAccessProperties}">Step over field access properties</CheckBox>
		</StackPanel>
	</Grid>
</UserControl>