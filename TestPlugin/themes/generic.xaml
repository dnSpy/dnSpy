<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:options="clr-namespace:TestPlugin"
>

    <DataTemplate DataType="{x:Type options:CustomOptionPage}">
        <StackPanel>
            <CheckBox IsChecked="{Binding UselessOption1}">Useless option 1</CheckBox>
            <Slider Minimum="0" Maximum="100" Value="{Binding UselessOption2}"/>
        </StackPanel>
    </DataTemplate>

</ResourceDictionary>